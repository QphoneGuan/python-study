# while 语句的用法！
i = 1
while true:
  print(i)
  i +=2
  if i >= 7:
    print('啊哦，一周过去了！')
  break
  
# return & def name(): 
def add_number(x,y):
  plus = x+y
  return plus
add_number(8,5)
  
# docstrings
def shout(word):
  """
  print a word with an exclamation mark following it.
  """
  print(word + '!')
shout('spam')

# Function as arguments
def plus1(x,y):
  return x+y
def plus2(func,x,y):
  return func(func(x,y),func(x,y))
a = 8
b = 10
print(plus2(plus1,a,b))

# import random module
import random
for i in range(5)
  value = random.randint(1,6)
  print(value)
  
# import certain item from module
from math import pi,sqrt
print(pi,4)

# rename "from import" with keyword AS
from math import sqrt as square_root
print(square_root(100))

